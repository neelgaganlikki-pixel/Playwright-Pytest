You are working on my existing OrangeHRM automation project:

Repository:
Playwright-Pytest

Technology:
- Python
- Playwright
- Pytest
- Page Object Model (POM)
- pytest-html
- Allure
- Jenkins
- Chromium

============================================================
IMPORTANT — FIRST INSPECT THE EXISTING PROJECT
============================================================

Before making ANY changes, inspect the complete existing project structure.

Understand and preserve the existing implementation for:

- conftest.py
- pytest.ini
- config/
- config_reader.py
- existing page objects
- existing tests
- existing fixtures
- existing logging
- screenshots
- videos
- traces
- Allure reporting
- JUnit reporting
- Jenkinsfile
- existing PIM functionality, if present
- existing login functionality

Do NOT create duplicate functionality if it already exists.

Do NOT remove or modify working tests unnecessarily.

Do NOT change the existing architecture.

Follow the existing project's coding style and naming conventions.

============================================================
OBJECTIVE
============================================================

Create a complete end-to-end OrangeHRM PIM Employee automation.

The test must:

1. Login to OrangeHRM as Admin.
2. Navigate to PIM.
3. Open Employee List.
4. Open Add Employee.
5. Generate random employee data.
6. Store the generated data in:
   
   test_data/user_data1.json

7. Upload an employee photo from:
   
   test_data/employee_photo.png

8. Fill the Add Employee form.
9. Enable Create Login Details.
10. Create login credentials for the employee.
11. Save the employee.
12. Verify the employee creation SUCCESS TOAST.
13. Fill the employee's Personal Details.
14. Fill Custom Fields.
15. Save Personal Details.
16. Verify the Personal Details SUCCESS TOAST.
17. Go back to Employee List.
18. Search for the exact employee created by this test.
19. Verify the employee exists and the data is correct.
20. Delete the exact employee.
21. Verify the DELETE SUCCESS TOAST.
22. Search again.
23. Verify the employee no longer exists.

The final flow must be:

LOGIN
 ↓
PIM
 ↓
EMPLOYEE LIST
 ↓
ADD EMPLOYEE
 ↓
GENERATE DATA
 ↓
STORE DATA IN user_data1.json
 ↓
UPLOAD PHOTO
 ↓
CREATE EMPLOYEE
 ↓
VERIFY CREATE TOAST
 ↓
PERSONAL DETAILS
 ↓
FILL PERSONAL DETAILS
 ↓
FILL CUSTOM FIELDS
 ↓
SAVE
 ↓
VERIFY UPDATE TOAST
 ↓
EMPLOYEE LIST
 ↓
SEARCH EMPLOYEE
 ↓
VERIFY EMPLOYEE
 ↓
DELETE EMPLOYEE
 ↓
VERIFY DELETE TOAST
 ↓
SEARCH AGAIN
 ↓
VERIFY EMPLOYEE DOES NOT EXIST


============================================================
ADMIN LOGIN
============================================================

Use these credentials to log into OrangeHRM:

Username:
Admin

Password:
admin123

These are the OrangeHRM administrator credentials.

Do NOT confuse them with the newly created employee's login credentials.

Reuse the existing login page object/fixture if the project already has one.

Do NOT create a second login implementation.


============================================================
EMPLOYEE LOGIN DETAILS
============================================================

The newly created employee must have:

Create Login Details:
Enabled

Username:
JohnKing0442

Status:
Enabled

Password:
JkAuto2026X9m4

Confirm Password:
EXACTLY THE SAME AS PASSWORD

Important:

The password must be stored in ONE variable.

Example concept:

password = "JkAuto2026X9m4"

Use the same variable for:

Password
Confirm Password

Do NOT generate a separate Confirm Password.

Do NOT log the password in the console.


============================================================
EMPLOYEE NAME DATA
============================================================

First Name:
RANDOM

Middle Name:
RANDOM

Last Name:
RANDOM

Do NOT hardcode:

John
Doe
King

Generate realistic fictional test names at runtime.

The generated names must contain alphabetic characters only.

The generated values must be stored in memory and reused throughout the complete test.


============================================================
GENDER REQUIREMENT
============================================================

Gender must NOT be independently randomized.

Gender must correspond consistently to the generated fictional FIRST NAME.

Use a controlled test-data name pool/category.

For example:

Male first-name pool:
David
Robert
Michael
James
Daniel
etc.

Female first-name pool:
Emily
Sarah
Jessica
Laura
Emma
etc.

If a male name is generated:

Gender = Male

If a female name is generated:

Gender = Female

Do NOT attempt to determine the gender of a real person.

This is purely controlled synthetic test data.

The selected Gender must be stored in the runtime test-data object and reused later.


============================================================
EMPLOYEE ID
============================================================

Generate a UNIQUE 4-digit Employee ID.

Example:

5831

Requirements:

- exactly 4 digits
- must not be regenerated during the test
- must be stored in the runtime test-data object
- must be stored in user_data1.json
- the exact same Employee ID must be used for:
  - employee creation
  - Personal Details
  - verification
  - search
  - deletion

Avoid collisions with existing employees where reasonably possible.


============================================================
OTHER EMPLOYEE DATA
============================================================

Generate the following at runtime:

Other ID:
Random valid alphanumeric value

Driver's License Number:
Random valid alphanumeric value

License Expiry Date:
Random valid FUTURE date

Date of Birth:
Random valid PAST date

Test_Field:
Random valid alphanumeric value


============================================================
DROPDOWN REQUIREMENT — VERY IMPORTANT
============================================================

DO NOT hardcode dropdown values blindly.

The automation must inspect the actual OrangeHRM dropdown options available in the UI.

This applies to:

- Nationality
- Marital Status
- Blood Type
- Gender, if implemented as a dropdown
- any other dropdown used by this test

For every dropdown that is marked RANDOM:

1. Open the dropdown.
2. Parse/read all available options from the DOM.
3. Remove placeholder options such as:
   
   -- Select --

4. Verify that valid options exist.
5. Select one valid option.
6. Store the selected option in the runtime test-data object.
7. Reuse the exact same selected value for later verification.

Do NOT invent dropdown values.

Do NOT assume the dropdown contains a specific value.

Do NOT select a value unless it actually exists in the current OrangeHRM UI.

Create reusable page-object functionality for reading dropdown options if appropriate.

For example, a reusable method could conceptually:

- open dropdown
- collect option texts
- return available options
- select a valid option

Follow the existing project's coding style.


============================================================
NATIONALITY
============================================================

Nationality must be selected dynamically from the actual options available in the OrangeHRM dropdown.

The test should:

- open Nationality dropdown
- parse available options
- ignore placeholder
- randomly select one valid option
- store the selected value
- use that same value throughout the test


============================================================
MARITAL STATUS
============================================================

Marital Status must be selected dynamically from the actual options available in the OrangeHRM dropdown.

The test should:

- open dropdown
- parse options
- ignore placeholder
- randomly select one valid option
- store the selected value
- reuse it for verification


============================================================
BLOOD TYPE
============================================================

Blood Type must be selected dynamically from the actual options available in the OrangeHRM dropdown.

The test should:

- open dropdown
- parse options
- ignore placeholder
- randomly select one valid option
- store the selected value
- reuse it for verification


============================================================
DATE REQUIREMENTS
============================================================

License Expiry Date:

- must be valid
- must be in the future
- must match the date format accepted by OrangeHRM

Date of Birth:

- must be valid
- must be in the past
- must match the date format accepted by OrangeHRM

Do not generate invalid dates.

Do not generate dates that OrangeHRM cannot accept.


============================================================
EMPLOYEE PHOTO
============================================================

Use a REAL image file.

Expected location:

test_data/employee_photo.png

The photo must NOT be the screenshot of the OrangeHRM application.

Upload the image during:

PIM
→ Employee List
→ Add Employee

Inspect the actual DOM and identify the file input used by OrangeHRM.

Use Playwright file upload functionality such as:

set_input_files()

Do NOT automate the Windows file chooser using mouse coordinates.

The JSON file should contain only the relative photo path.

Example:

"photo": {
    "path": "test_data/employee_photo.png"
}

Do NOT store the image as Base64 in JSON.


============================================================
JSON STORAGE — VERY IMPORTANT
============================================================

Create:

test_data/user_data1.json

IMPORTANT:

user_data1.json is an OUTPUT/STORAGE file.

It is NOT the source of random employee values.

Do NOT read "RANDOM" values from JSON and then generate them.

The correct flow is:

Python
 ↓
Generate random values
 ↓
Store values in memory
 ↓
Write ACTUAL values to user_data1.json
 ↓
Use the same runtime values throughout the test

The JSON must contain the ACTUAL values used in that execution.

NEVER write:

"first_name": "RANDOM"

NEVER write:

"employee_id": "GENERATE_4_DIGIT"

NEVER write:

"gender": "DERIVED_FROM_FIRST_NAME"

Instead, after execution it should contain real values.

Example:

{
    "login": {
        "username": "Admin",
        "password": "admin123"
    },
    "employee": {
        "first_name": "David",
        "middle_name": "Michael",
        "last_name": "Wilson",
        "employee_id": "5831",
        "gender": "Male",
        "other_id": "X7K29P",
        "driver_license_number": "DL84921",
        "license_expiry_date": "20-12-2030",
        "nationality": "Cambodian",
        "marital_status": "Single",
        "date_of_birth": "14-06-1998"
    },
    "employee_login": {
        "create_login_details": true,
        "username": "JohnKing0442",
        "status": "Enabled",
        "password": "JkAuto2026X9m4",
        "confirm_password": "JkAuto2026X9m4"
    },
    "custom_fields": {
        "blood_type": "O+",
        "test_field": "TestX82"
    },
    "photo": {
        "path": "test_data/employee_photo.png"
    }
}

The actual values in the JSON will be different because employee data is generated dynamically.


============================================================
JSON WRITE BEHAVIOR
============================================================

Implement a clean utility/function for writing the generated test data.

The function must:

- create test_data directory if necessary
- create/update user_data1.json
- write valid JSON
- write the complete actual test data
- overwrite the previous execution's employee data
- not write placeholders
- preserve valid JSON formatting

The generated values must first exist in the runtime test-data object.

Then write that object to user_data1.json.

Do NOT retrieve the random values from the JSON during the same execution.


============================================================
RUNTIME DATA CONSISTENCY
============================================================

Generate the values ONCE.

Store them in one runtime test-data structure.

For example:

first_name
middle_name
last_name
employee_id
gender
other_id
driver_license_number
license_expiry_date
nationality
marital_status
date_of_birth
blood_type
test_field

Once generated, these values must NEVER be regenerated during:

- employee creation
- Personal Details
- verification
- search
- deletion
- final verification

The exact same values must be reused.


============================================================
ADD EMPLOYEE PAGE
============================================================

Navigate:

PIM
→ Employee List
→ Add Employee

Fill:

First Name:
generated first name

Middle Name:
generated middle name

Last Name:
generated last name

Employee ID:
generated 4-digit ID

Upload:

test_data/employee_photo.png

Enable:

Create Login Details

Then fill:

Username:
JohnKing0442

Status:
Enabled

Password:
JkAuto2026X9m4

Confirm Password:
same password value

Click:

Save


============================================================
CREATION TOAST VERIFICATION
============================================================

Immediately after clicking Save:

1. Wait for the OrangeHRM success toast.
2. Verify that it is visible.
3. Verify that it indicates successful employee creation.
4. Do NOT continue until the creation success toast has been verified.

The test must NOT consider creation successful merely because Save was clicked.

Inspect the actual OrangeHRM DOM to determine the correct toast locator and text.

Use:

expect()

Do NOT use:

page.wait_for_timeout()


============================================================
PERSONAL DETAILS
============================================================

After successful employee creation and creation toast verification:

Fill the employee Personal Details.

Employee ID:
Use the SAME generated Employee ID.

Other ID:
Use generated random value.

Driver's License Number:
Use generated random value.

License Expiry Date:
Use generated future date.

Nationality:
Use dynamically discovered valid dropdown option.

Marital Status:
Use dynamically discovered valid dropdown option.

Date of Birth:
Use generated valid past date.

Gender:
Use the gender derived from the generated first-name category.


============================================================
CUSTOM FIELDS
============================================================

Fill:

Blood Type:
Use dynamically discovered valid dropdown option.

Test_Field:
Use generated random value.


============================================================
PERSONAL DETAILS SAVE
============================================================

Click the appropriate Personal Details Save button.

Immediately:

1. Wait for success toast.
2. Verify toast visibility.
3. Verify that it indicates successful update/save.
4. Only then continue.

Do NOT use arbitrary sleeps.


============================================================
EMPLOYEE VERIFICATION
============================================================

Navigate to:

PIM
→ Employee List

Search for the exact employee created by the test.

Use the stored runtime values.

Prefer searching by:

Employee ID

and/or

the generated employee name.

Verify:

First Name
Middle Name
Last Name
Employee ID

If the employee details page allows verification of additional saved values, verify:

Other ID
Driver's License Number
License Expiry Date
Nationality
Marital Status
Date of Birth
Gender
Blood Type
Test_Field

Do not generate new values for verification.


============================================================
DELETE EMPLOYEE
============================================================

Delete ONLY the employee created by this test.

Identify the employee using the stored:

First Name
Middle Name
Last Name
Employee ID

Do not delete another employee.

Perform the delete operation.

If a confirmation dialog appears:

- verify it
- confirm deletion

After deletion:

1. Wait for the delete success toast.
2. Verify the toast is visible.
3. Verify it indicates successful deletion.


============================================================
FINAL DELETION VERIFICATION
============================================================

After deletion:

Return to Employee List.

Search using the same generated Employee ID/name.

Verify that the employee no longer exists.

The test must fail if the employee is still present.


============================================================
PAGE OBJECT MODEL
============================================================

Follow the existing POM architecture.

If a suitable Employee/PIM page object already exists:

- extend it
- reuse it
- do not create duplicate methods unnecessarily

If one does not exist, create the appropriate page object.

Potential structure:

pages/
    employee_page.py

tests/
    pim/
        test_employee.py

test_data/
    user_data1.json
    employee_photo.png

Adapt names to the existing project conventions.


============================================================
PAGE OBJECT RESPONSIBILITIES
============================================================

Page objects should contain UI interactions.

Possible methods:

click_pim()

click_employee_list()

click_add_employee()

enter_employee_name(first_name, middle_name, last_name)

enter_employee_id(employee_id)

upload_employee_photo(photo_path)

enable_create_login_details()

enter_username(username)

select_status(status)

enter_password(password)

enter_confirm_password(password)

save_employee()

verify_employee_created_toast()

enter_other_id(other_id)

enter_driver_license(driver_license)

enter_license_expiry_date(date)

select_nationality(nationality)

select_marital_status(marital_status)

enter_date_of_birth(date)

select_gender(gender)

select_blood_type(blood_type)

enter_test_field(value)

save_personal_details()

verify_personal_details_saved_toast()

search_employee(...)

verify_employee(...)

delete_employee(...)

verify_employee_deleted_toast()

verify_employee_not_present(...)

Adjust method names to match the existing framework.


============================================================
LOCATOR STRATEGY
============================================================

Use robust Playwright locators.

Preferred order:

1. get_by_role()
2. get_by_label()
3. get_by_placeholder()
4. stable attributes
5. CSS/XPath only when necessary

Avoid:

- screen coordinates
- nth-child where avoidable
- generated CSS classes
- fragile XPath
- arbitrary sleeps


============================================================
ASSERTIONS
============================================================

Use Playwright expect().

Verify:

- PIM page loaded
- Employee List loaded
- Add Employee loaded
- Create Login Details enabled
- employee photo upload accepted where possible
- employee creation success
- creation success toast
- Personal Details saved
- Personal Details success toast
- employee appears in Employee List
- correct employee data
- deletion performed on correct employee
- delete success toast
- employee no longer exists


============================================================
NO ARBITRARY WAITS
============================================================

DO NOT use:

page.wait_for_timeout()

Use:

- Playwright auto-waiting
- expect()
- locator.wait_for()
- appropriate explicit waits when genuinely necessary


============================================================
LOGGING
============================================================

Follow existing logging conventions.

Useful logs:

Starting PIM employee creation test.
Logging into OrangeHRM as Admin.
Opening PIM.
Opening Employee List.
Opening Add Employee.
Generated employee test data.
Employee ID generated: XXXX.
Uploading employee photo.
Filling employee details.
Creating employee login details.
Saving employee.
Employee creation success toast verified.
Opening Personal Details.
Filling Personal Details.
Saving Personal Details.
Personal Details success toast verified.
Searching for employee.
Employee verified successfully.
Starting employee deletion.
Delete success toast verified.
Verifying employee deletion.
Employee deletion verified successfully.

NEVER print passwords in logs.


============================================================
JENKINS COMPATIBILITY
============================================================

The test must work with the existing Jenkins pipeline.

Environment:

Python 3.14
Playwright
Pytest
Chromium
Windows
Jenkins
Headless execution

Existing command:

python -m pytest tests -v -s --junitxml=test-results\pytest-results.xml

The new test must be automatically discovered.

Do not introduce dependencies unless absolutely necessary.

Do not modify Jenkinsfile unless genuinely required.


============================================================
REPORTING
============================================================

Preserve compatibility with:

- JUnit XML
- pytest HTML
- Allure
- screenshots on failure
- video on failure
- trace on failure

Use the existing configuration.


============================================================
FAILURE / CLEANUP
============================================================

The test should not leave created employees behind.

The normal test flow MUST explicitly delete the employee.

If a failure occurs after employee creation but before normal deletion:

- attempt cleanup if appropriate
- do not hide the original failure
- do not replace the original exception with a cleanup exception

Follow the existing project architecture for cleanup.


============================================================
EXISTING TESTS
============================================================

Do NOT remove or break:

- existing Login tests
- existing Buzz tests
- existing Vacancy tests
- existing fixtures
- existing reporting
- existing configuration

The new PIM Employee test must coexist with the existing suite.


============================================================
FILES TO CREATE/MODIFY
============================================================

First inspect the repository.

Then determine the minimum required changes.

Create:

test_data/user_data1.json

Create/use:

test_data/employee_photo.png

Create or modify the appropriate Employee Page Object.

Create or modify the appropriate PIM Employee test.

Do NOT modify unrelated files.


============================================================
COMPLETE FILE CONTENT REQUIREMENT
============================================================

IMPORTANT:

Whenever you create or modify a file, provide the COMPLETE contents of that file.

Do NOT provide:

- partial snippets
- individual methods only
- "add this line"
- abbreviated code
- omitted sections

I need complete replacement files that I can copy directly into my project.

Preserve the existing formatting and coding style as much as possible.


============================================================
FINAL RESPONSE FROM COPILOT
============================================================

After implementation, provide:

1. Files created.
2. Files modified.
3. Complete contents of EVERY created/modified file.
4. Explain how random employee data is generated.
5. Explain how gender is derived consistently from the synthetic first-name category.
6. Explain how dropdown options are parsed from the actual OrangeHRM UI.
7. Explain how generated values are stored in user_data1.json.
8. Confirm that user_data1.json is OUTPUT/STORAGE and NOT the source of random values.
9. Explain the employee photo upload.
10. Explain the creation success toast verification.
11. Explain the Personal Details success toast verification.
12. Explain the delete success toast verification.
13. Explain the final employee-not-found verification.
14. Give the exact command to execute the new test.
15. Give the exact command to execute the complete suite.
16. Confirm existing tests were preserved.
17. List any assumptions made about OrangeHRM selectors or UI behavior.

Do not make unrelated framework changes.





                 GENERATE
                    ↓
             Runtime Test Data
                    ↓
             user_data1.json
              (STORE ACTUAL DATA)
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
   Create Employee       Personal Details
        ↓                       ↓
     Verify                  Save
        ↓                       ↓
        └───────────┬───────────┘
                    ↓
               Search/Verify
                    ↓
                  Delete
                    ↓
             Verify Delete Toast
                    ↓
             Search Again
                    ↓
             Employee NOT Found
                    ↓
                PASSED